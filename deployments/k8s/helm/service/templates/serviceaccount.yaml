{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "myy-service.serviceAccountName" . }}
  labels:
    {{- include "myy-service.labels" . | nindent 4 }}
{{- end }}

