apiVersion: v1
kind: Service
metadata:
  name: {{ include "myy-service.fullname" . }}
  labels:
    {{- include "myy-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "myy-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

