syntax = "proto3";

package user.v1;

option go_package = "github.com/myy-chat/backend/api/user/v1;v1";
option java_multiple_files = true;
option java_package = "user.v1";

import "google/api/annotations.proto";
import "google/protobuf/timestamp.proto";

// ============================================
// User Service Definition
// ============================================

service UserService {
  // Register 用户注册 (FR-001)
  rpc Register(RegisterRequest) returns (RegisterResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/register"
      body: "*"
    };
  }

  // Login 用户登录 (FR-003)
  rpc Login(LoginRequest) returns (LoginResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/login"
      body: "*"
    };
  }

  // Logout 用户登出
  rpc Logout(LogoutRequest) returns (LogoutResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/logout"
      body: "*"
    };
  }

  // LogoutAll 用户退出所有设备
  rpc LogoutAll(LogoutAllRequest) returns (LogoutAllResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/logout-all"
      body: "*"
    };
  }

  // RefreshToken 刷新令牌
  rpc RefreshToken(RefreshTokenRequest) returns (RefreshTokenResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/refresh"
      body: "*"
    };
  }

  // GetUser 获取当前用户信息
  rpc GetUser(GetUserRequest) returns (GetUserResponse) {
    option (google.api.http) = {
      get: "/api/v1/users/me"
    };
  }

  // UpdateUser 更新用户信息
  rpc UpdateUser(UpdateUserRequest) returns (UpdateUserResponse) {
    option (google.api.http) = {
      put: "/api/v1/users/me"
      body: "*"
    };
  }

  // UpdatePassword 修改密码 (FR-003c)
  rpc UpdatePassword(UpdatePasswordRequest) returns (UpdatePasswordResponse) {
    option (google.api.http) = {
      put: "/api/v1/users/me/password"
      body: "*"
    };
  }

  // GetProfile 获取用户画像
  rpc GetProfile(GetProfileRequest) returns (GetProfileResponse) {
    option (google.api.http) = {
      get: "/api/v1/users/me/profile"
    };
  }

  // UpdateProfile 更新用户画像
  rpc UpdateProfile(UpdateProfileRequest) returns (UpdateProfileResponse) {
    option (google.api.http) = {
      put: "/api/v1/users/me/profile"
      body: "*"
    };
  }

  // RequestAccountDeletion 请求账号删除 (FR-004a)
  rpc RequestAccountDeletion(RequestAccountDeletionRequest) returns (RequestAccountDeletionResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/me/delete-request"
      body: "*"
    };
  }

  // CancelAccountDeletion 取消账号删除 (FR-004b)
  rpc CancelAccountDeletion(CancelAccountDeletionRequest) returns (CancelAccountDeletionResponse) {
    option (google.api.http) = {
      delete: "/api/v1/users/me/delete-request"
    };
  }

  // VerifyEmail 验证邮箱 (FR-002)
  rpc VerifyEmail(VerifyEmailRequest) returns (VerifyEmailResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/verify-email"
      body: "*"
    };
  }

  // ResendVerification 重发验证邮件
  rpc ResendVerification(ResendVerificationRequest) returns (ResendVerificationResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/resend-verification"
      body: "*"
    };
  }

  // ForgotPassword 忘记密码
  rpc ForgotPassword(ForgotPasswordRequest) returns (ForgotPasswordResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/forgot-password"
      body: "*"
    };
  }

  // ResetPassword 重置密码
  rpc ResetPassword(ResetPasswordRequest) returns (ResetPasswordResponse) {
    option (google.api.http) = {
      post: "/api/v1/users/reset-password"
      body: "*"
    };
  }

  // AddCredits 内部积分增加
  rpc AddCredits(AddCreditsRequest) returns (AddCreditsResponse);

  // DeductCredits 内部积分扣减
  rpc DeductCredits(DeductCreditsRequest) returns (DeductCreditsResponse);

  // ReserveCredits 内部积分预扣
  rpc ReserveCredits(ReserveCreditsRequest) returns (ReserveCreditsResponse);

  // SettleReservedCredits 内部预扣结算
  rpc SettleReservedCredits(SettleReservedCreditsRequest) returns (SettleReservedCreditsResponse);

  // ReleaseReservedCredits 内部预扣释放
  rpc ReleaseReservedCredits(ReleaseReservedCreditsRequest) returns (ReleaseReservedCreditsResponse);

  // GetCreditBalance 内部积分余额查询
  rpc GetCreditBalance(GetCreditBalanceRequest) returns (GetCreditBalanceResponse);
}

// ============================================
// Register Messages
// ============================================

message RegisterRequest {
  string username = 1;       // 用户名 (3-50字符)
  string email = 2;          // 邮箱
  string password = 3;       // 密码 (8-128字符)
  string phone = 4;          // 手机号 (可选)
}

message RegisterResponse {
  int64 user_id = 1;         // 用户ID (Snowflake)
  string access_token = 2;   // JWT访问令牌
  string refresh_token = 3 [deprecated = true];  // 已弃用：改为 HttpOnly Cookie 传输
  int64 expires_in = 4;      // 访问令牌过期时间 (秒)
  bool email_verification_required = 5;  // 是否需要邮箱验证
}

// ============================================
// Login Messages
// ============================================

message LoginRequest {
  string email = 1;          // 邮箱或用户名
  string password = 2;       // 密码
}

message LoginResponse {
  int64 user_id = 1;
  string access_token = 2;
  string refresh_token = 3 [deprecated = true];  // 已弃用：改为 HttpOnly Cookie 传输
  int64 expires_in = 4;
  User user = 5;             // 用户基本信息
}

// ============================================
// Logout Messages
// ============================================

message LogoutRequest {}

message LogoutResponse {
  bool success = 1;
}

message LogoutAllRequest {}

message LogoutAllResponse {
  bool success = 1;
}

// ============================================
// Token Refresh Messages
// ============================================

message RefreshTokenRequest {
  // 已弃用：refresh token 由浏览器通过 HttpOnly Cookie 自动携带。
  // 为兼容旧客户端暂时保留，后续版本删除。
  string refresh_token = 1 [deprecated = true];
}

message RefreshTokenResponse {
  string access_token = 1;
  string refresh_token = 2 [deprecated = true];  // 已弃用：新 refresh token 通过 Set-Cookie 下发
  int64 expires_in = 3;
}

// ============================================
// Get User Messages
// ============================================

message GetUserRequest {
  // user_id 从 JWT 获取，无需传入
}

message GetUserResponse {
  User user = 1;
}

// ============================================
// Update User Messages
// ============================================

message UpdateUserRequest {
  string username = 1;       // 新用户名 (可选)
  string phone = 2;          // 新手机号 (可选)
  string avatar_url = 3;     // 新头像URL (可选)
}

message UpdateUserResponse {
  User user = 1;
}

// ============================================
// Password Messages
// ============================================

message UpdatePasswordRequest {
  string current_password = 1;  // 当前密码
  string new_password = 2;      // 新密码
}

message UpdatePasswordResponse {
  bool success = 1;
}

message ForgotPasswordRequest {
  string email = 1;
}

message ForgotPasswordResponse {
  bool success = 1;
  string message = 2;
}

message ResetPasswordRequest {
  string token = 1;          // 重置令牌
  string new_password = 2;   // 新密码
}

message ResetPasswordResponse {
  bool success = 1;
}

// ============================================
// Internal Credit Messages
// ============================================

message AddCreditsRequest {
  int64 user_id = 1;
  int64 amount = 2;
  string reason = 3;
  string reference_type = 4;
  string reference_id = 5;
  string idempotency_key = 6;
}

message AddCreditsResponse {
  bool success = 1;
  int64 balance_after = 2;
}

message DeductCreditsRequest {
  int64 user_id = 1;
  int64 amount = 2;
  string reason = 3;
  string reference_type = 4;
  string reference_id = 5;
  string idempotency_key = 6;
}

message DeductCreditsResponse {
  bool success = 1;
  int64 balance_after = 2;
}

message ReserveCreditsRequest {
  int64 user_id = 1;
  int64 amount = 2;
  string reason = 3;
  string reference_type = 4;
  string reference_id = 5;
  string idempotency_key = 6;
}

message ReserveCreditsResponse {
  bool success = 1;
  int64 reserve_id = 2;
  int64 balance_after = 3;
  int64 reserved_balance_after = 4;
}

message SettleReservedCreditsRequest {
  int64 user_id = 1;
  int64 reserve_id = 2;
  int64 amount = 3;
  string reason = 4;
  string reference_type = 5;
  string reference_id = 6;
  string idempotency_key = 7;
}

message SettleReservedCreditsResponse {
  bool success = 1;
  int64 balance_after = 2;
  int64 reserved_balance_after = 3;
}

message ReleaseReservedCreditsRequest {
  int64 user_id = 1;
  int64 reserve_id = 2;
  int64 amount = 3;
  string reason = 4;
  string reference_type = 5;
  string reference_id = 6;
  string idempotency_key = 7;
}

message ReleaseReservedCreditsResponse {
  bool success = 1;
  int64 balance_after = 2;
  int64 reserved_balance_after = 3;
}

message GetCreditBalanceRequest {
  int64 user_id = 1;
}

message GetCreditBalanceResponse {
  int64 user_id = 1;
  int64 balance = 2;
  int64 reserved_balance = 3;
  int64 total_recharged = 4;
  int64 total_consumed = 5;
  google.protobuf.Timestamp updated_at = 6;
}

// ============================================
// Profile Messages
// ============================================

message GetProfileRequest {}

message GetProfileResponse {
  UserProfile profile = 1;
}

message UpdateProfileRequest {
  string full_name = 1;      // 全名
  string gender = 2;         // 性别: male, female, other
  string birth_date = 3;     // 生日 (YYYY-MM-DD)
  Location location = 4;     // 位置
  repeated string interests = 5;  // 兴趣爱好
  string occupation = 6;     // 职业
  string bio = 7;            // 个人简介
}

message UpdateProfileResponse {
  UserProfile profile = 1;
}

// ============================================
// Account Deletion Messages
// ============================================

message RequestAccountDeletionRequest {
  string password = 1;       // 确认密码
  string reason = 2;         // 删除原因 (可选)
}

message RequestAccountDeletionResponse {
  bool success = 1;
  google.protobuf.Timestamp deletion_scheduled_at = 2;  // 计划删除时间 (30天后)
}

message CancelAccountDeletionRequest {}

message CancelAccountDeletionResponse {
  bool success = 1;
}

// ============================================
// Email Verification Messages
// ============================================

message VerifyEmailRequest {
  string token = 1;          // 验证令牌
}

message VerifyEmailResponse {
  bool success = 1;
}

message ResendVerificationRequest {
  string email = 1;
}

message ResendVerificationResponse {
  bool success = 1;
  string message = 2;
}

// ============================================
// Common Types
// ============================================

// User 用户基本信息
message User {
  int64 id = 1;              // 用户ID (Snowflake)
  string username = 2;       // 用户名
  string email = 3;          // 邮箱
  string phone = 4;          // 手机号
  string avatar_url = 5;     // 头像URL
  bool email_verified = 6;   // 邮箱是否验证
  int64 credit_balance = 7;  // 积分余额
  google.protobuf.Timestamp created_at = 8;
  google.protobuf.Timestamp last_login_at = 9;
  bool deletion_scheduled = 10;  // 是否已请求删除
}

// UserProfile 用户画像
message UserProfile {
  int64 user_id = 1;
  string full_name = 2;
  string gender = 3;
  string birth_date = 4;     // YYYY-MM-DD
  Location location = 5;
  repeated string interests = 6;
  string occupation = 7;
  string bio = 8;
  google.protobuf.Timestamp updated_at = 9;
}

// Location 位置信息
message Location {
  string country = 1;
  string city = 2;
  string province = 3;
}
